output:
  logstash:
    enabled: true
    hosts:
      - elk:5044
    timeout: 15
    tls:
      certificate_authorities:
      - /etc/pki/tls/certs/logstash-beats.crt

filebeat:
  prospectors:
    -
      paths:
        - /var/lib/btcd/mainnet/btcd.log
      include_lines: ['BMGR']
      tail_files: true
      input_type: log
      document_type: beat
